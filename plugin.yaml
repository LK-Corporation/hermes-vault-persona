name: vault-persona
version: 0.1.0
description: Load persona-bot profiles from a markdown vault as system-prompt overlays via /persona <slug>
author: Brus
provides_hooks:
  - pre_llm_call
  - on_session_reset
